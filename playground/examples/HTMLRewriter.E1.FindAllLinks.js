// Generated by ReScript, PLEASE EDIT WITH CARE


let rewriter = new HTMLRewriter();

function findInternalLinks(html) {
  let internalLinks = [];
  rewriter.on("*", {
    element: element => {
      let match = element.tagName;
      let match$1 = element.getAttribute("href");
      if (match === "a" && match$1 !== null && match$1.startsWith("/")) {
        internalLinks.push(match$1);
        return;
      }
      
    }
  }).transform(new Response(html));
  return internalLinks;
}

let content = await fetch("https://bun.sh");

let html = await content.text();

let internalLinks = findInternalLinks(html);

console.log(internalLinks);

export {
  rewriter,
  findInternalLinks,
  content,
  html,
  internalLinks,
}
/* rewriter Not a pure module */
