// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Nodepath from "node:path";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";

let path = Nodepath.resolve(import.meta.dir, "test.txt");

let file = Bun.file(path);

let resp = new Response(file);

console.log(Primitive_option.fromNullable(resp.headers.get("Content-Type")));

let _server = Bun.serve({
  fetch: async (req, _server) => new Response(Bun.file(new URL(req.url).pathname))
});

export {
  path,
  file,
  resp,
  _server,
}
/* path Not a pure module */
